## Automatic proxy configuraion
This is a script which configures the apt.conf file with the proxy authentication in linux

## Running it

```
chmod +x proxy.py
sudo ./proxy.py
```
Make sure python3 is installed on your system
